let qq_number = "63175sos8924";
/**
 * 
 * @param {*} qq  type String
 * @return type boolean
 * @功能 判断QQ号是否合格
 * @desc 只能是数字 长度5位号以上，13位以内
 */
const is_valid_qq=(qq)=>{
    if(!qq) return false;
    // console.log(qq);
    if()
    return true;
};
is_valid_qq(qq_number);